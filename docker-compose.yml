version: '3.8'

services:
  web:
    build: .
    container_name: lebari_django_web
    command: python manage.py runserver 0.0.0.0:8001
    volumes:
      - .:/app
    ports:
      - "8001:8001"
    environment:
      - DEBUG=1
